<div class="pallete">
  <canvas id="canvas" width="800" height="600"></canvas>

  <%= form_tag({:action => :create}, {:remote => true}) do %>
    <%= submit_tag "Save" %>
  <% end %>
</div>

<ul>
  <% @pictures.each do |picture| %>
    <li>
      <%= tag("img", :src => picture.data, :width => 300) %>
    </li>
  <% end %>
</ul>
